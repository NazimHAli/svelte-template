name: Artifacts

on: [push]

jobs:
  artifacts:
    runs-on: ubuntu-latest
    name: Screenshot
    steps:
      - uses: actions/checkout@v2.3.1
      - uses: cypress-io/github-action@v2
        with:
          spec: cypress/integration/screenshot/homepage.spec.js

      - uses: actions/upload-artifact@v2.1.3
        with:
          path: cypress/screenshots
